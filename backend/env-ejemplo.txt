# ── Llaves. Genera cada una con:
#    python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=pon-aqui-una-cadena-larga-y-aleatoria
JWT_SECRET_KEY=otra-cadena-larga-y-distinta

# ── Base de datos (desarrollo local)
DB_USER=sioat
DB_PASS=tu_password
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=sioat

# ── En producción con MySQL gestionado (Aiven, TiDB, etc.)
# Puedes usar las cinco variables de arriba, o una sola cadena:
# DATABASE_URL=mysql://usuario:password@host:puerto/basededatos
#
# Esos servicios exigen TLS con su propia autoridad certificadora. Descarga el
# ca.pem del panel del proveedor y pega su contenido aquí (o usa DB_SSL_CA con
# una ruta si el archivo está en disco):
# DB_SSL_CA_CONTENT=-----BEGIN CERTIFICATE-----\nMIIE...\n-----END CERTIFICATE-----
# DB_SSL_CA=/ruta/al/ca.pem

# ── Frontend autorizado. URL exacta, sin diagonal final. Separa varias con coma.
CORS_ORIGINS=http://localhost:5173

# ── Restringe el registro al correo institucional. Vacío = cualquier correo.
DOMINIO_PERMITIDO=

# ---------------------------------------------------------------------------
# CÓMO USAR ESTE ARCHIVO
#   1. Renómbralo (o cópialo) a  backend/.env  — con punto al inicio.
#   2. Llena DB_PASS y las dos llaves secretas.
#   El .env NO se sube a git. Este env-ejemplo.txt sí.
# ---------------------------------------------------------------------------
